{
  "name": "ecf-vite-et-gourmand",
  "private": true,
  "scripts": {
    "vercel-build": "git clone --depth 1 https://github.com/PHPMailer/PHPMailer.git vendor/phpmailer/phpmailer && mkdir -p public/asset && cp -r asset/* public/asset/"
  }
}
